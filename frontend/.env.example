# Leave VITE_API_URL unset for local dev (Vite proxies /api to localhost:4000)
# and for Vercel when vercel.json rewrites /api to the backend.
#
# Set it only if you want the browser to call Render directly:
# VITE_API_URL=https://jc-command-center-api.onrender.com
